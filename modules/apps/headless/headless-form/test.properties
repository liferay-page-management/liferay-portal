##
## Test Suites
##

    modified.files.includes[relevant][headless-form-rule]=**

    relevant.rule.names=\
        headless-form-rule

    test.batch.names[relevant][headless-form-rule]=\
        functional-tomcat90-postgresql155-jdk8

    #
    # Portal Hotfix Release
    #

    test.batch.run.property.query[functional-bundle-tomcat-postgresql155-jdk8][test-portal-hotfix-release]=\
        ${test.batch.run.property.query[functional-tomcat*-postgresql*-jdk8][relevant][headless-form-rule]}

    #
    # Relevant
    #

    test.batch.run.property.query[functional-tomcat*-postgresql*-jdk8][relevant][headless-form-rule]=\
        (portal.acceptance == true) AND \
        (testray.main.component.name ~ "Forms")

##
## Testray
##

testray.main.component.name=Forms