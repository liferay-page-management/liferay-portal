##
## Test Suites
##

    modified.files.includes[relevant][headless-admin-user-rule]=**

    relevant.rule.names=\
        headless-admin-user-rule

    test.batch.names[relevant][headless-admin-user-rule]=\
        functional-tomcat90-postgresql155-jdk8

    #
    # Portal Hotfix Release
    #

    test.batch.run.property.query[functional-bundle-tomcat-postgresql155-jdk8][test-portal-hotfix-release]=\
        ${test.batch.run.property.query[functional-tomcat*-postgresql*-jdk8][relevant][headless-admin-user-rule]}

    #
    # Relevant
    #

    test.batch.run.property.query[functional-tomcat*-postgresql*-jdk8][relevant][headless-admin-user-rule]=\
        (portal.acceptance == true) AND \
        (testray.main.component.name ~ "Headless USM")

##
## Testray
##

    testray.main.component.name=Headless USM