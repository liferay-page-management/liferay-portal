##
## Test Suites
##

    modified.files.includes[relevant][site-initializer-masterclass-rule]=**

    relevant.rule.names=\
        site-initializer-masterclass-rule

    test.batch.names[relevant][site-initializer-masterclass-rule]=\
        functional-tomcat90-postgresql155-jdk8

    #
    # Portal Hotfix Release
    #

    test.batch.run.property.query[functional-bundle-tomcat-postgresql155-jdk8][test-portal-hotfix-release]=\
        ${test.batch.run.property.query[functional-tomcat*-postgresql*-jdk8][relevant][site-initializer-masterclass-rule]}

    #
    # Relevant
    #

    test.batch.run.property.query[functional-tomcat*-postgresql*-jdk8][relevant][site-initializer-masterclass-rule]=\
        (portal.acceptance == true) AND \
        (\
            (testray.component.names ~ "Masterclass") OR \
            (testray.main.component.name ~ "Masterclass")\
        )