##
## Test Suites
##

    modified.files.includes[relevant][frontend-taglib-clay-sample-web-rule]=**

    relevant.rule.names=\
        frontend-taglib-clay-sample-web-rule

    test.batch.names[relevant][frontend-taglib-clay-sample-web-rule]=\
        functional-tomcat90-postgresql155-jdk8

    #
    # Portal Hotfix Release
    #

    test.batch.run.property.query[functional-bundle-tomcat-postgresql155-jdk8][test-portal-hotfix-release]=\
        ${test.batch.run.property.query[functional-tomcat*-postgresql*-jdk8][relevant][frontend-taglib-clay-sample-web-rule]}

    #
    # Relevant
    #

    ignoreParents[relevant][frontend-taglib-clay-sample-web-rule]=true

    test.batch.run.property.query[functional-tomcat*-postgresql*-jdk8][relevant][frontend-taglib-clay-sample-web-rule]=\
        (testray.component.names == "Clay") OR \
        (\
            (environment.acceptance == true) AND \
            (portal.acceptance == true) AND \
            (portal.upstream == true) AND \
            (\
                (app.server.types == null) OR \
                (app.server.types ~ "tomcat")\
            ) AND \
            (\
                (database.types == null) OR \
                (database.types ~ "postgresql")\
            ) AND \
            (\
                (operating.system.types == null) OR \
                (operating.system.types ~ "centos")\
            )\
        )

##
## Testray
##

    testray.main.component.name=User Interface